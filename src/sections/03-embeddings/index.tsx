import { Callout, Formula, M, SectionHero, Summary, Term } from '@/components/ui';
import { SceneContainer } from '@/components/three-shell/SceneContainer';
import { embeddingStore } from '@/scenes/EmbeddingSpace/store';
import { CosinePlayground } from './widgets/CosinePlayground';
import { VectorArithmetic } from './widgets/VectorArithmetic';
import { EmbeddingMatrixLookup } from './widgets/EmbeddingMatrixLookup';
import { EmbeddingOverlay } from './widgets/EmbeddingOverlay';

export default function EmbeddingsSection() {
  return (
    <>
      <SectionHero
        id="embeddings"
        learn={['Как номер токена превращается в вектор и что этот вектор означает', 'Как измерить «похожесть» слов через косинус угла', 'Почему направления в пространстве смыслов тоже что-то значат']}
      />

      <h2>Номер — это ещё не смысл</h2>
      <p>
        После токенизации у нас есть номера: «кошка» — 2, «собака» — 3. Но номера ничего не говорят о смысле: 2 и 3 стоят рядом не потому, что кошка похожа на
        собаку. Нужен способ представить токен так, чтобы похожие по смыслу были <em>близки</em>, а разные — <em>далеки</em>.
      </p>
      <p>
        Решение: каждому токену поставить в соответствие <Term id="vector">вектор</Term> — список из нескольких сотен чисел. Такой вектор называется{' '}
        <Term id="embedding">эмбеддингом</Term>. Все эмбеддинги хранятся в одной большой таблице: строка с номером токена и есть его вектор.
      </p>
      <Formula tex={String.raw`e_t = E[x_t], \qquad E \in \mathbb{R}^{|V| \times d}`} caption={<>Матрица E имеет |V| строк (по числу токенов в словаре) и d столбцов (размерность эмбеддинга, обычно 768–16 384). Взять эмбеддинг — значит просто достать строку номер xₜ.</>} />
      <EmbeddingMatrixLookup />

      <h2>Смысл как геометрия</h2>
      <p>
        Числа в векторах подбираются при обучении так, чтобы модель лучше предсказывала текст. Оказывается, для этого выгодно, чтобы слова, встречающиеся в
        похожих контекстах, получали похожие векторы. В итоге «кошка» и «собака» оказываются рядом, а «кошка» и «налоговая декларация» — далеко.
      </p>
      <Callout kind="try">
        В сцене ниже 84 слова, спроецированные в три измерения. Наведите курсор на точку, нажмите на неё, чтобы увидеть соседей, и обязательно запустите
        экскурсию — она проведёт по всем идеям раздела.
      </Callout>
      <SceneContainer
        sceneId="embedding-space"
        title="Пространство эмбеддингов"
        store={embeddingStore}
        overlay={<EmbeddingOverlay />}
        legend={<p>Цвет точки — смысловая группа. Линии от выбранного слова ведут к пяти ближайшим соседям, толщина — косинусная близость. Ось Y здесь не «важнее» других — это просто три числа из многих.</p>}
      />

      <h2>Как измерить близость</h2>
      <p>
        Слово «близко» нужно уточнить. Самая популярная мера — <Term id="cosine">косинусная близость</Term>: косинус угла между векторами. Она не зависит от длины
        векторов, только от направления, и удобно ложится на интуицию: 1 — «то же самое», 0 — «никак не связано».
      </p>
      <Formula tex={String.raw`\cos\theta = \frac{a \cdot b}{\|a\|\,\|b\|} = \frac{\sum_i a_i b_i}{\sqrt{\sum_i a_i^2}\,\sqrt{\sum_i b_i^2}}`} caption={<>Скалярное произведение делим на произведение длин. В d измерениях формула такая же, просто сумм больше.</>} />
      <CosinePlayground />

      <h2>Направления тоже значат</h2>
      <p>
        Самое удивительное свойство: не только расстояния, но и <em>направления</em> в этом пространстве осмысленны. Вектор от «мужчина» к «король» — это что-то
        вроде «стать монархом». Прибавьте его к «женщина» — получите точку рядом с «королева».
      </p>
      <Formula tex={String.raw`e_{\text{king}} - e_{\text{man}} + e_{\text{woman}} \approx e_{\text{queen}}`} />
      <VectorArithmetic />
      <Callout kind="info" title="Три измерения вместо тысячи">
        Наши векторы трёхмерные, чтобы их можно было нарисовать. У настоящих моделей d — тысячи, и там таких «осмысленных направлений» очень много одновременно:
        род, число, время, тональность, тема. Все они умещаются в один вектор, потому что <M tex="d" /> велико.
      </Callout>

      <Summary>
        <ul>
          <li>Эмбеддинг — вектор из d чисел, строка обучаемой матрицы E. Похожие по употреблению слова получают похожие векторы.</li>
          <li>Близость измеряют косинусом угла: он не зависит от длины вектора и лежит в [−1, 1].</li>
          <li>Направления в пространстве тоже несут смысл, поэтому работает арифметика вроде «король − мужчина + женщина».</li>
        </ul>
      </Summary>
    </>
  );
}
