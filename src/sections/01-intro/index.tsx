import { Link } from 'react-router-dom';
import { Callout, Formula, M, SectionHero, Summary, Term } from '@/components/ui';
import { NextWordPredictor } from './widgets/NextWordPredictor';
import { AutoregressiveLoop } from './widgets/AutoregressiveLoop';

export default function IntroSection() {
  return (
    <>
      <SectionHero
        id="intro"
        learn={['Что именно делает языковая модель и почему это «всего лишь» предсказание следующего токена', 'Как из одного предсказания получается целый текст', 'Из каких частей состоит путь от текста к ответу — карта курса']}
      />

      <h2>Одна задача — предсказать следующий токен</h2>
      <p>
        Всё, что умеет <Term id="llm">большая языковая модель</Term>, сводится к одному действию. Ей дают начало текста, а она отвечает: «вот насколько вероятно каждое из
        возможных продолжений». Не «правильный ответ», а именно <Term id="distribution">распределение вероятностей</Term> по всем словам, которые она знает.
      </p>
      <p>
        Это звучит слишком просто для системы, которая пишет код и объясняет теорему Пифагора. Но чтобы хорошо предсказывать следующее слово в миллионах разных
        текстов, приходится «выучить» грамматику, факты, стиль, логику и даже немного здравого смысла. Всё это возникает как побочный эффект одной задачи.
      </p>
      <Callout kind="hint">
        Представьте автодополнение на телефоне, которое прочитало весь интернет и помнит контекст на тысячи слов назад. Это и есть LLM — по крайней мере, по
        устройству.
      </Callout>

      <Callout kind="try">
        Ниже — крошечная модель, обученная на 90 предложениях. Введите начало фразы и посмотрите, что она предложит. Попробуйте «кот», «собака бежит», «утром на
        улице».
      </Callout>
      <NextWordPredictor />

      <h2>Из предсказания — в текст</h2>
      <p>
        Модель выдаёт вероятности только для <em>одного</em> следующего токена. Чтобы получить целый ответ, приходится крутить цикл: предсказали, выбрали токен,
        приклеили его к тексту, предсказали снова. Это называется <Term id="autoregressive">авторегрессией</Term>.
      </p>
      <Formula
        tex={String.raw`P(x_1, x_2, \dots, x_T) = \prod_{t=1}^{T} P(x_t \mid x_1, \dots, x_{t-1})`}
        caption={
          <>
            Вероятность целого текста раскладывается в произведение вероятностей каждого токена при условии всех предыдущих. Модель умеет считать только множитель{' '}
            <M tex={String.raw`P(x_t \mid x_{<t})`} /> — но этого достаточно, чтобы собрать всё остальное.
          </>
        }
      />
      <Callout kind="try">Пройдите цикл по шагам. Обратите внимание: на каждом обороте модель заново смотрит на весь накопленный контекст.</Callout>
      <AutoregressiveLoop />

      <h2>Где здесь «понимание»?</h2>
      <p>
        Наша игрушечная модель просто считает: после «кот сидит на» в корпусе чаще всего шло «окне». Настоящая LLM делает то же самое, но не таблицей частот, а
        нейросетью с миллиардами <Term id="parameters">параметров</Term>, которая умеет обобщать: она никогда не видела вашу фразу целиком, но видела миллионы
        похожих.
      </p>
      <p>
        Между «считать частоты» и «обобщать» — всё содержание этого курса. Вот его карта:
      </p>
      <ol>
        <li>
          <Link to="/tokenization">Токенизация</Link> — как текст превращается в числа.
        </li>
        <li>
          <Link to="/embeddings">Эмбеддинги</Link> — как числа становятся точками в пространстве смыслов.
        </li>
        <li>
          <Link to="/attention">Внимание</Link> — как токены обмениваются информацией.
        </li>
        <li>
          <Link to="/transformer">Трансформер</Link> — как из блоков внимания собирается модель.
        </li>
        <li>
          <Link to="/training">Обучение</Link> — откуда берутся параметры.
        </li>
        <li>
          <Link to="/sampling">Генерация</Link> — как из вероятностей рождается текст.
        </li>
        <li>
          <Link to="/finetuning">Дообучение</Link> — почему модель отвечает, а не продолжает.
        </li>
        <li>
          <Link to="/prompting">Промпты и контекст</Link> — как этим пользоваться и где границы.
        </li>
      </ol>
      <Callout kind="info" title="Про игрушечные модели">
        Все демонстрации в курсе считаются прямо в браузере на маленьких моделях, которые можно понять целиком. Они устроены проще настоящих, но показывают те же
        самые идеи. Там, где упрощение существенно, мы об этом скажем.
      </Callout>

      <Summary>
        <ul>
          <li>LLM — это функция «начало текста → вероятности следующего токена». Больше ничего она не делает.</li>
          <li>Текст получается циклом: предсказать, выбрать, добавить, повторить.</li>
          <li>«Умные» способности — следствие того, что хорошо предсказывать текст очень трудно без знаний о мире.</li>
        </ul>
      </Summary>
    </>
  );
}
