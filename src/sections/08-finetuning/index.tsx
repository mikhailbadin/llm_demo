import { Callout, Formula, M, SectionHero, Summary, Term } from '@/components/ui';
import { PipelineStepper } from './widgets/PipelineStepper';
import { PreferenceTrainer } from './widgets/PreferenceTrainer';
import { KlPenaltyExplorer } from './widgets/KlPenaltyExplorer';

export default function FinetuningSection() {
  return (
    <>
      <SectionHero
        id="finetuning"
        learn={['Почему предобученная модель продолжает текст, а не отвечает на вопросы', 'Что такое SFT, модель наград и RLHF', 'Откуда берутся «характер» ассистента и его ограничения']}
      />

      <h2>Базовая модель не хочет вам помогать</h2>
      <p>
        После предобучения модель умеет одно: продолжать текст так, как он продолжался бы в интернете. Спросите её «Как заварить чай?» — и она с равным успехом
        может выдать ещё пять вопросов, рекламу чайника или оглавление форума. Она не «отвечает», потому что нигде не училась отвечать.
      </p>
      <p>Чтобы превратить предсказатель текста в ассистента, нужны ещё несколько этапов. Идея всех — сдвинуть распределение модели в сторону «хороших ответов».</p>
      <PipelineStepper />

      <h2>SFT: показать, как надо</h2>
      <p>
        Первый шаг — <Term id="sft">supervised fine-tuning</Term>. Люди пишут тысячи пар «инструкция → образцовый ответ», и модель дообучают на них тем же
        способом, что и раньше: предсказывать следующий токен. Только теперь тексты — это диалоги в нужном формате. Модель быстро усваивает форму: отвечать по
        делу, структурировать, обращаться к собеседнику.
      </p>
      <Callout kind="info" title="Почему не остановиться на SFT">
        Написать хороший ответ трудно и дорого, а разметчики не идеальны. Зато <em>сравнить</em> два ответа и сказать, какой лучше, — быстро и надёжно. На этом
        строится следующий этап.
      </Callout>

      <h2>Модель наград: научиться сравнивать</h2>
      <p>
        Разметчикам показывают вопрос и два ответа модели, а они выбирают лучший. На таких сравнениях обучают отдельную{' '}
        <Term id="reward-model">модель наград</Term>: она получает текст и выдаёт число — насколько ответ «нравится людям». Вероятность предпочтения задаётся моделью
        Брэдли–Терри:
      </p>
      <Formula tex={String.raw`P(A \succ B) = \sigma(r_A - r_B) = \frac{1}{1 + e^{-(r_A - r_B)}}`} caption={<>Если награда A больше награды B на 2, модель считает, что люди предпочтут A с вероятностью ≈ 88 %. Обучение подгоняет r так, чтобы эти вероятности совпадали с реальными выборами.</>} />
      <PreferenceTrainer />

      <h2>RLHF: оптимизировать награду, но не слишком</h2>
      <p>
        Теперь у нас есть автоматический «судья». Основную модель дообучают так, чтобы её ответы получали от судьи высокую оценку — это{' '}
        <Term id="rlhf">RLHF</Term>, обучение с подкреплением на человеческой обратной связи. Но есть ловушка: судья несовершенен, и если давить на него слишком
        сильно, модель найдёт способ его обмануть. Поэтому в целевую функцию добавляют штраф за отход от базовой модели:
      </p>
      <Formula tex={String.raw`\max_\pi \; \mathbb{E}_{y \sim \pi}[\,r(x, y)\,] - \beta \cdot \operatorname{KL}\big(\pi \,\|\, \pi_{\text{ref}}\big)`} caption={<>Первый член — средняя награда за ответы, второй — KL-расстояние до исходной модели, умноженное на β. Метод <Term id="dpo">DPO</Term> решает ту же задачу без отдельной модели наград, напрямую на парах «лучше / хуже».</>} />
      <KlPenaltyExplorer />
      <Callout kind="hint" title="Откуда «характер»">
        Всё, что мы воспринимаем как личность ассистента — вежливость, отказы от опасных просьб, склонность соглашаться, — результат этих этапов. Это не свойство
        предобучения, а следствие того, какие ответы разметчики выбирали чаще. Совокупность таких методов называют <Term id="alignment">alignment</Term>.
      </Callout>
      <p>
        Важно помнить: после всех этапов модель всё ещё делает ровно то же самое — предсказывает следующий токен. Просто её распределение <M tex="\pi" /> теперь
        устроено так, что самые вероятные продолжения — это полезные ответы.
      </p>

      <Summary>
        <ul>
          <li>Базовая модель продолжает текст; чтобы она отвечала, её дообучают на инструкциях (SFT).</li>
          <li>Модель наград учится на сравнениях пар ответов и заменяет людей при оценке миллионов ответов.</li>
          <li>RLHF/DPO сдвигают модель к высоким наградам, а штраф KL не даёт ей «обмануть» судью.</li>
        </ul>
      </Summary>
    </>
  );
}
