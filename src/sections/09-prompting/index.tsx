import { Callout, SectionHero, Summary, Term } from '@/components/ui';
import { ContextWindowVisualizer } from './widgets/ContextWindowVisualizer';
import { ChatTemplateBuilder } from './widgets/ChatTemplateBuilder';
import { PromptTechniquesTabs } from './widgets/PromptTechniquesTabs';
import { HallucinationDemo } from './widgets/HallucinationDemo';

export default function PromptingSection() {
  return (
    <>
      <SectionHero
        id="prompting"
        learn={['Что такое контекстное окно и почему модель «забывает»', 'Как устроен чат с точки зрения модели', 'Какие приёмы промптинга работают и почему, и где проходят границы возможностей']}
      />

      <h2>Всё, что есть у модели, — это контекст</h2>
      <p>
        Модель не помнит прошлых разговоров, не имеет доступа к интернету и не знает, что происходило после её обучения. Единственное, что она видит, — это
        последовательность токенов на входе: <Term id="context-window">контекстное окно</Term>. Оно конечно, и всё, что в него не влезло, для модели не
        существует.
      </p>
      <Callout kind="try">Добавьте в диалог одно-два сообщения или уменьшите окно. Посмотрите, в какой момент модель «забудет», как зовут собеседника.</Callout>
      <ContextWindowVisualizer />

      <h2>Чат — это тоже просто текст</h2>
      <p>
        Роли «система», «пользователь», «ассистент» выглядят как отдельная структура, но для модели это один длинный текст со служебными токенами.{' '}
        <Term id="system-prompt">Системный промпт</Term> — просто фрагмент в начале, который модель приучили считать инструкцией. Ответ ассистента — продолжение
        после токена «assistant».
      </p>
      <ChatTemplateBuilder />

      <h2>Приёмы, которые работают</h2>
      <p>
        Раз модель продолжает текст, то <Term id="prompt">промпт</Term> — это способ сделать нужное продолжение самым вероятным. Отсюда все приёмы: показать
        примеры (<Term id="few-shot">few-shot</Term>), попросить рассуждать по шагам (<Term id="chain-of-thought">цепочка рассуждений</Term>), подложить документы (
        <Term id="rag">RAG</Term>).
      </p>
      <PromptTechniquesTabs />

      <h2>Границы</h2>
      <p>
        Понимание того, как модель устроена, объясняет и её слабости. Она оптимизирует правдоподобие текста, а не истинность, поэтому может уверенно выдумать
        факт — это <Term id="hallucination">галлюцинация</Term>. Она не знает событий после <Term id="knowledge-cutoff">даты отсечки</Term>. Она плохо считает
        буквы, потому что видит токены. И каждый токен стоит денег и времени.
      </p>
      <HallucinationDemo />
      <Callout kind="warning" title="Как с этим жить">
        Давайте модели факты в контексте, а не полагайтесь на её память. Просите ссылки и проверяйте их. Для точных вычислений давайте инструменты (калькулятор,
        код). И помните: беглость текста ничего не говорит о его правдивости.
      </Callout>

      <Summary>
        <ul>
          <li>Модель видит только контекстное окно. Что не поместилось — не существует.</li>
          <li>Чат, роли и системный промпт — это текст со служебными токенами; ответ — его продолжение.</li>
          <li>Промптинг делает нужный ответ самым вероятным продолжением; галлюцинации — плата за то, что модель оптимизирует правдоподобие, а не истину.</li>
        </ul>
      </Summary>
    </>
  );
}
