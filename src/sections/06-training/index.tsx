import { Callout, Formula, M, SectionHero, Summary, Term } from '@/components/ui';
import { SceneContainer } from '@/components/three-shell/SceneContainer';
import { lossStore } from '@/scenes/LossSurface/store';
import { CrossEntropyPlayground } from './widgets/CrossEntropyPlayground';
import { NgramTrainer } from './widgets/NgramTrainer';
import { GradientDescent1D } from './widgets/GradientDescent1D';
import { LossOverlay } from './widgets/LossOverlay';
import { LossDriver } from './widgets/LossDriver';

export default function TrainingSection() {
  return (
    <>
      <SectionHero
        id="training"
        learn={['Почему для обучения не нужна разметка: текст сам себе учитель', 'Как измеряют ошибку модели (cross-entropy и perplexity)', 'Как градиентный спуск находит хорошие параметры и что такое скорость обучения']}
      />

      <h2>Текст сам себе учитель</h2>
      <p>
        Чтобы обучить модель, нужны примеры «вход → правильный ответ». Для предсказания следующего токена такие примеры делаются из любого текста бесплатно:
        берём «кот сидит на», правильный ответ — «окне». Каждая позиция в каждом тексте — отдельный пример. Так за <Term id="pretraining">предобучение</Term>{' '}
        модель видит триллионы примеров, и никто их не размечал.
      </p>
      <Callout kind="try">
        Ниже — «обучение» нашей игрушечной модели. Она учится счётом: сколько раз какое слово шло после какого. Двигайте ползунок и смотрите, как с ростом
        данных падает ошибка на предложениях, которых модель не видела.
      </Callout>
      <NgramTrainer />

      <h2>Как измерить ошибку</h2>
      <p>
        Модель выдаёт вероятности. Хорошая модель даёт правильному следующему токену высокую вероятность. Значит, ошибку логично измерять так: чем меньше
        вероятность правильного ответа, тем больше штраф. Эта <Term id="loss">функция потерь</Term> называется <Term id="cross-entropy">cross-entropy</Term>.
      </p>
      <Formula tex={String.raw`\mathcal{L} = -\frac{1}{T}\sum_{t=1}^{T} \log p_\theta(x_t \mid x_{<t})`} caption={<>Средний минус-логарифм вероятности правильного токена по всем позициям текста. θ — параметры модели.</>} />
      <Formula tex={String.raw`\text{PPL} = e^{\mathcal{L}}`} caption={<><Term id="perplexity">Perplexity</Term> — та же величина, но в понятных единицах: «между сколькими равновероятными вариантами модель как будто выбирает». PPL = 1 — идеальное знание текста; у моделей уровня GPT-2 на английском PPL ≈ 10–20, у современных больших — единицы.</>} />
      <CrossEntropyPlayground />

      <h2>Спуск по ландшафту</h2>
      <p>
        Потери зависят от параметров. Представьте потери как высоту ландшафта, а параметры — как координаты. Обучение — это спуск в низину.{' '}
        <Term id="gradient-descent">Градиентный спуск</Term> работает просто: вычисляем, в какую сторону потери растут быстрее всего (градиент), и делаем
        маленький шаг в противоположную. Размер шага — <Term id="learning-rate">скорость обучения</Term>.
      </p>
      <Formula tex={String.raw`\theta \leftarrow \theta - \eta \nabla_\theta \mathcal{L}`} caption={<>η — скорость обучения. Градиент считают автоматически (backpropagation) сразу по всем миллиардам параметров.</>} />
      <GradientDescent1D />
      <Callout kind="try">
        Теперь то же самое в двух измерениях. Нажмите на поверхность, чтобы поставить шарик, и запускайте спуск. Попробуйте большую скорость обучения — шарик
        начнёт метаться. Экскурсия покажет всё по порядку.
      </Callout>
      <SceneContainer
        sceneId="loss-surface"
        title="Ландшафт функции потерь"
        store={lossStore}
        overlay={<LossOverlay />}
        legend={<p>Высота и цвет — величина потерь для пары параметров. Красная стрелка — минус градиент (куда идти). Белая линия — пройденный путь. У настоящей модели измерений миллиарды, но алгоритм ровно тот же.</p>}
      />
      <LossDriver />

      <h2>Масштаб</h2>
      <p>
        Один шаг обучения — это прогнать через модель пачку текстов, посчитать loss, вычислить градиент и сдвинуть параметры. Больших моделей это касается в
        промышленных масштабах: сотни тысяч шагов, триллионы токенов, тысячи GPU и недели времени. Главное открытие последних лет: loss предсказуемо падает с ростом
        модели, данных и вычислений — так называемые законы масштабирования. Именно поэтому модели становятся всё больше.
      </p>
      <Callout kind="warning" title="Чего здесь нет">
        Мы не показываем backpropagation — алгоритм, который вычисляет градиент по всем параметрам сразу. Для интуиции достаточно знать, что он существует и
        стоит примерно вдвое дороже прямого прохода. Для модели <M tex="\theta" /> — это те самые матрицы из предыдущих разделов.
      </Callout>

      <Summary>
        <ul>
          <li>Обучение не требует разметки: каждое слово в тексте — правильный ответ для предыдущих.</li>
          <li>Ошибку измеряет cross-entropy (минус логарифм вероятности правильного токена); perplexity — её удобный экспонент.</li>
          <li>Градиентный спуск сдвигает параметры против градиента; скорость обучения задаёт длину шага, и её легко переборщить.</li>
        </ul>
      </Summary>
    </>
  );
}
