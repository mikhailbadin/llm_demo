import { Callout, Formula, SectionHero, Summary, Term } from '@/components/ui';
import { SceneContainer } from '@/components/three-shell/SceneContainer';
import { probStore } from '@/scenes/ProbabilityLandscape/store';
import { SamplingPlayground } from './widgets/SamplingPlayground';
import { GreedyVsSampling } from './widgets/GreedyVsSampling';
import { TemperatureStrip } from './widgets/TemperatureStrip';
import { ProbOverlay } from './widgets/ProbOverlay';

export default function SamplingSection() {
  return (
    <>
      <SectionHero
        id="sampling"
        learn={['Чем жадный выбор отличается от сэмплирования и почему модели сэмплируют', 'Что на самом деле делает температура', 'Как top-k и top-p отрезают «хвост» и зачем']}
      />

      <h2>Из вероятностей — в слово</h2>
      <p>
        Модель выдала распределение. Дальше надо выбрать один токен, и есть два принципиально разных способа. <Term id="greedy">Жадный</Term>: всегда брать
        самый вероятный. <Term id="sampling">Сэмплирование</Term>: бросить «взвешенную» монетку и взять случайный токен пропорционально вероятности.
      </p>
      <p>
        Жадный выбор кажется логичным: берём самое вероятное — что может пойти не так? На практике он даёт предсказуемый и однообразный текст: на один и тот же
        запрос всегда получается один и тот же ответ. У больших моделей к этому добавляется склонность зацикливаться и повторять целые фразы. Случайность нужна,
        чтобы текст был живым и чтобы разные запуски давали разные варианты.
      </p>
      <p className="small muted">
        В нашей игрушечной модели предложения короткие, поэтому зацикливания не увидеть — зато хорошо видно главное отличие: слева всегда одно и то же, справа
        каждый раз новое.
      </p>
      <GreedyVsSampling />

      <h2>Температура</h2>
      <p>
        Между «всегда лучшее» и «честная случайность» есть целая шкала. Её задаёт <Term id="temperature">температура</Term>: перед softmax все{' '}
        <Term id="logits">логиты</Term> делят на T.
      </p>
      <Formula tex={String.raw`p_i = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}`} caption={<>T → 0: распределение стягивается к лидеру (жадный выбор). T = 1: как есть. T → ∞: все токены равновероятны — шум.</>} />
      <TemperatureStrip />

      <h2>Обрезать хвост: top-k и top-p</h2>
      <p>
        Даже при разумной температуре у распределения длинный хвост: тысячи токенов с ничтожной вероятностью. Каждый по отдельности маловероятен, но вместе они
        набирают несколько процентов, и рано или поздно модель «вытянет» какую-нибудь ерунду. Поэтому хвост отрезают.
      </p>
      <ul>
        <li>
          <Term id="top-k">Top-k</Term>: оставить k самых вероятных токенов, остальные обнулить, оставшиеся перенормировать.
        </li>
        <li>
          <Term id="top-p">Top-p</Term> (nucleus): оставить наименьший набор токенов, суммарная вероятность которых не меньше p. Число оставшихся меняется
          само: где модель уверена — их мало, где сомневается — много.
        </li>
      </ul>
      <Callout kind="try">
        Крутите ползунки и смотрите, какие столбики сереют. Добавляйте слова по одному — «Сэмплировать» — и следите, как распределение перестраивается под новый
        контекст.
      </Callout>
      <SamplingPlayground />

      <h2>То же самое в объёме</h2>
      <SceneContainer
        sceneId="probability-landscape"
        title="Распределение следующего токена"
        store={probStore}
        overlay={<ProbOverlay />}
        legend={<p>Столбики — 20 самых вероятных слов после текущего текста; высота — итоговая вероятность после температуры и отсечки. Серые — отсечённые top-k/top-p, красная заслонка — граница отсечки. Каркасные контуры — высоты при T = 1. Кнопка «Сэмплировать» роняет шарик на случайно выбранное слово (выбор идёт по всему словарю, поэтому изредка оно оказывается правее показанных столбиков).</p>}
      />
      <Callout kind="hint" title="Что выбирают на практике">
        Для чата: T ≈ 0,7–1,0 и top-p ≈ 0,9. Для кода и точных ответов: T ≈ 0–0,3. Для мозгового штурма: T ≈ 1,2. Ползунок «креативность» в интерфейсах — это
        обычно температура.
      </Callout>

      <Summary>
        <ul>
          <li>Жадный выбор детерминирован и однообразен; сэмплирование даёт разнообразие ценой случайности, которой управляет <Term id="seed">seed</Term>.</li>
          <li>Температура делит логиты перед softmax: низкая — острее, высокая — равномернее. Порядок кандидатов она не меняет.</li>
          <li>Top-k и top-p отрезают хвост маловероятных токенов, чтобы случайность не превращалась в бессмыслицу.</li>
        </ul>
      </Summary>
    </>
  );
}
