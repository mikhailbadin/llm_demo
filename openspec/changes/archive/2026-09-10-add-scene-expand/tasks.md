## 1. Каркас развёрнутого режима

- [x] 1.1 Добавить хук `src/hooks/useScrollLock.ts` (пока флаг включён — класс `is-scroll-locked` на `body`, на выходе снимается; сами `overflow: hidden` и `overscroll-behavior: none` объявлены в CSS). Проверка: `npm run typecheck` проходит; хук вызывается в п. 1.3.
- [x] 1.2 Добавить в `src/styles/global.css` рядом с блоком `.scene*`: `.scene-slot`, `.scene--expanded` (`position: fixed; top/left/right: 0; height: 100dvh; z-index: 50; display: flex; flex-direction: column`, без скруглений и внешних отступов), `.scene--expanded .scene__viewport { flex: 1; height: auto; }` (одного правила хватает на всех ширинах: два класса бьют `.scene__viewport { height: 340px }`, медиазапрос специфичности не добавляет), правило скрытия подсказки, тач-гарда и легенды, расширенные `.scene--expanded .tour` / `.scene--expanded .scene__overlay` в `@media (min-width: 721px)` и поправку положения панели в `@media (max-width: 720px)`. Отступы сцены перенести с `.scene` на `.scene-slot`, иначе поток теряет их в момент ухода секции в `fixed` и прокрутка сдвигается. Проверка: `npm run lint` проходит, новых цветов не добавлено (`git diff src/styles/theme.ts` пуст).
- [x] 1.3 В `SceneContainer.tsx` завести состояние `slotHeight: number | null` (`expanded = slotHeight !== null`), обернуть `<section class="scene">` в `<div class="scene-slot">` и вешать класс `scene--expanded`; перед разворачиванием мерить слот через `getBoundingClientRect().height` и отдавать число в состояние, откуда оно попадает в `style` обёртки; подключить `useScrollLock(expanded)`. Проверка: на `/llm_demo/attention` сцена разворачивается на весь экран, страница за ней не прокручивается, после сворачивания текст раздела остаётся на прежнем месте прокрутки.
- [x] 1.4 Добавить в тулбар кнопку «⛶ Развернуть» / «✕ Свернуть» общим компонентом `Button` (рендерить только при доступном WebGL), держать на ней ref для возврата фокуса; для этого разрешить `Button` принимать `ref` (`ComponentPropsWithRef<'button'>` в `src/components/ui/Button.tsx`). Проверка: на `/llm_demo/embeddings` кнопка переключает режим и меняет подпись; при эмуляции браузера без WebGL (заглушить `useWebGLSupport`) кнопки нет, запасной текст и список шагов экскурсии на месте.

## 2. Выход из режима и клавиатура

- [x] 2.1 Сделать единственным владельцем Escape слушатель `keydown` на `document`: «свой или чужой» решать проверкой `ref.current.contains(document.activeElement)`, порядок веток — «идёт экскурсия → завершить экскурсию, иначе развёрнуто → свернуть, иначе → погасить `cameraActive`»; у `onKeyDown` вьюпорта оставить только ← →. Проверка: на `/llm_demo/transformer` в развёрнутой сцене Esc при активной экскурсии закрывает только экскурсию, повторный Esc сворачивает сцену; одно нажатие не делает и то и другое.
- [x] 2.2 Завести запись в истории через `navigate(pathname, { state: { …, sceneExpanded: sceneId } })` при разворачивании, сворачивать сцену, когда метка исчезает из `location.state`, и уходить из режима через `navigate(-1)` по кнопке и по Esc. Проверка: на `/llm_demo/training` кнопка «назад» в браузере сворачивает сцену и оставляет раздел открытым; после трёх разворачиваний и сворачиваний «назад» уводит на предыдущий раздел, а не отматывает лишние записи.
- [x] 2.3 Свести режим и метку в истории одной сверкой: пока `marked === expanded` — ничего не делать, разошлись — либо свернуться («назад» убрал метку), либо вычистить метку, пережившую перезагрузку (`navigate(pathname, { replace: true, state: … })`). Проверка: развернуть сцену на `/llm_demo/sampling`, нажать F5 — раздел открывается со свёрнутой сценой.
- [x] 2.4 Возвращать фокус на кнопку разворачивания при сворачивании и уводить его на `.scene__viewport` при разворачивании. Проверка: пройти сценарий только с клавиатуры — Tab до кнопки, Enter, Esc, фокус снова на кнопке (видна рамка `:focus-visible`).

## 3. Поведение внутри развёрнутого кадра

- [x] 3.1 При разворачивании ставить `setCameraActive(true)`; тач-гард, подсказку «нажмите на сцену» и легенду прятать правилом в CSS по классу режима, а не условиями в разметке; панели «Управление» дать отдельную память для развёрнутого кадра (`overlayOpenExpanded`), чтобы она была там открыта по умолчанию и не оставалась открытой после сворачивания. Проверка: на узком экране (DevTools, 390 px) развёрнутая сцена сразу показывает панель управления, тач-гарда нет, колесо мыши масштабирует без предварительного клика.
- [x] 3.2 Убедиться, что состояние сцены переживает переключение режима: параметры, выбранный объект и ракурс те же, что были. Проверка: на `/llm_demo/embeddings` сдвинуть слайдер, выбрать точку, развернуть и свернуть — значения и выделение сохранились; кнопка «⟲ Камера» подгоняет ракурс под текущий кадр.
- [x] 3.3 Проверить экскурсию в развёрнутом виде на всех пяти сценах (`embeddings`, `attention`, `transformer`, `training`, `sampling`): карточка шага, перелёты камеры, подсветка, ← → и точки-шаги. Проверка: каждая экскурсия проходится до конца в развёрнутом виде.

## 4. Сквозная проверка

- [x] 4.1 Проверить, что развёрнутая сцена перекрывает шапку и сайдбар, а поповеры остаются поверх неё: открыть подсказку «?» в развёрнутой сцене. Проверка: содержимое поповера видно целиком, шапка и сайдбар скрыты под сценой.
- [x] 4.2 Проверить, что у предков сцены не появилось стилей, ломающих `position: fixed`: `grep -nE "^\.(app|main|content|prose) \{[^}]*(transform|filter|backdrop-filter|contain|will-change)" src/styles/global.css` — должно быть пусто.
- [x] 4.3 Проверить границу импортов three: `grep -rln "from 'three'\|from '@react-three" src | grep -v "^src/components/three/\|^src/scenes/[A-Za-z]*/index.tsx"` — должно быть пусто.
- [x] 4.4 Прогнать `npm run lint && npm run typecheck && npm test && npm run build`; убедиться, что входной чанк вырос меньше чем на килобайт и чанк three.js в выводе `vite build` не изменился (по факту: входной 94,72 КБ gz и three 250,1 КБ gz — без изменений, вырос только CSS на 0,14 КБ gz).
- [x] 4.5 Проверить собранную версию: `npm run preview`, пройти по `/llm_demo/embeddings`, `/llm_demo/attention`, `/llm_demo/transformer`, `/llm_demo/training`, `/llm_demo/sampling` и развернуть каждую сцену — в том числе в эмуляции сенсорного экрана (страница под сценой не прокручивается жестом).

## 5. Чистка после ревью

- [x] 5.1 Мемоизировать элемент сцены (`useMemo(() => <Scene store={store} active={visible} />, …)`), чтобы ререндер оболочки на шаг экскурсии, `cameraActive` или смену режима не проходил по всему дереву three.js. Проверка: `npm run lint` и `npm run typecheck` проходят, сцена по-прежнему реагирует на параметры и экскурсию в браузере.
- [x] 5.2 Убрать дубли, найденные ревью: мёртвое правило `.scene--expanded .scene__viewport` в медиазапросе, продублированные `right`/`width` в узком блоке, ручную склейку классов кнопки, второй обработчик Escape и реф-детектор «был развёрнут». Проверка: `npm run lint`, `npm run typecheck`, `npm test` проходят; `grep -rln "from 'three'\|from '@react-three" src | grep -v "^src/components/three/\|^src/scenes/[A-Za-z]*/index.tsx"` пусто.
- [x] 5.3 Перепроверить поведение после чистки на собранной версии: разворачивание на всех пяти разделах, три способа выхода, Esc при экскурсии, блокировка прокрутки, позиция прокрутки и фокус, состояние сцены, узкий экран и режим без WebGL. Проверка: наведение показывает подпись, клик выбирает объект, перетаскивание вращает сцену, колесо масштабирует сразу после разворачивания — всё в развёрнутом кадре, консоль без ошибок.
